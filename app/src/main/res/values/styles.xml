<resources>
    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">--> <!--Material Desing-->
        <!--Antes de Material Desing se ocupaba Holo-->
    <!--<style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar"> -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar"> <!--Material Theming-->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--<item name="android:actionBarStyle">@style/MyActionBar</item> Holo-->
    </style>

    <style name="MyTitleTextStyle" parent="android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        TextAppearance.Holo.Widget.ActionBar.Title
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="MyActionBar" parent="android:Widget.Holo.ActionBar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:titleTextStyle">@style/MyTitleTextStyle</item>
    </style>

</resources>